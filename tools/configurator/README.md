# Autograph Configurator

A simple python tool to set or replace values in autograph configuration files.

You probably don't want to use this. It's a helper to set stuff up in docker compose and CI.
